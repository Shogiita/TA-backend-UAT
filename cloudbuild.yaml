steps:
  # 1. Build Docker image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/ta-backend-api", "."]

  # 2. Push image ke Google Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/ta-backend-api"]

  # 3. Deploy ke Google Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "ta-backend-api"
      - "--image"
      - "gcr.io/$PROJECT_ID/ta-backend-api"
      - "--region"
      - "asia-southeast2"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
      - "--port"
      - "8080"
      # Tambahkan minimal instances untuk mencegah cold start jika diperlukan
      - "--min-instances"
      - "1"

images:
  - "gcr.io/$PROJECT_ID/ta-backend-api"
